---
- name: Создание сети
  community.docker.docker_network:
    name: "{{ networkName }}"
    driver: overlay

- name: Выкладка сервиса
  include: "../services/{{ item }}/service.yml"
  vars:
    - name: "{{ item }}"
  loop: "{{ services }}"
