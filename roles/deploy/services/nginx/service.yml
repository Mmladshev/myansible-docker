---
- name: "[{{ name }}] Конфигурация кофнига"
  include: "../config.yml"
  loop:
    - nginx.conf
  loop_cintrol:
    loop_var: config_item

- name: "[{{ name }}] Выкладка сервиса"
  block:
    - name: "[{{ name }}] Выкладка сервиса"
      community.docker.docker_swarm_service:
        name: "{{ name }}"
        image: "nginx:{{ version }}"
        state: present
        networks:
          - name: app_network
        publish:
          - mode: ingress
            protocol: tcp
            published_port: 80
            target_port: 80
        configs:
          - config_name: "nginx.conf"
            filename: "/etc/nginx/nginx.conf"
  tags: "{{ name }}"
